import { Box, Typography } from "@mui/material";
import React from "react";

const ProductChart = () => {
    return (
        <Box>
            <Typography variant="h1" sx={{ mx: 12, my: 12 }}>
                Products
            </Typography>
        </Box>
    );
};

export default ProductChart;
